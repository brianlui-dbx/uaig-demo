# Copy to .env for local development: cp .env.example .env
DATABRICKS_CONFIG_PROFILE=dbw-brlui-sandbox
# DATABRICKS_HOST=https://adb-7405619474463800.0.azuredatabricks.net
# DATABRICKS_TOKEN=dapi....

# MLflow experiment to log traces/models to (numeric id).
MLFLOW_EXPERIMENT_ID=

MLFLOW_TRACKING_URI="databricks"
MLFLOW_REGISTRY_URI="databricks-uc"

# MapleChain wiring — Model Service FQN (routed via the Unity AI Gateway)
MODEL_SERVICE=catalog_sandbox_y049iu.uaig_demo.maplechain_custom_ms
UC_CATALOG=catalog_sandbox_y049iu
UC_SCHEMA=uaig_demo
CUSTOM_MCP_URL=
